{%- comment -%}
  DEPRECATED 2026-05-30 — file kept empty because Shopify CLI cannot
  always delete theme files cleanly from this environment. Safe to
  remove manually from the repo.

  The "Log in to see pricing" gate has been removed from the theme.
  Customer-tier price access is now handled by an external app tied
  to customer registration, not by theme Liquid.

  Do not re-introduce this snippet. If you need a logged-out CTA in
  the future, add it through the app, not here.
{%- endcomment -%}
