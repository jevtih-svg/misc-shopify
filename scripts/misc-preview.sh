#!/bin/bash
# ============================================================
# misc-preview.sh — DEPRECATED 2026-05-30
#
# The misc_dev_show_price toggle has been removed. The theme no
# longer hides prices behind a customer-login gate; price-tier
# access is now handled by an external app tied to customer
# registration.
#
# This file is kept as a stub so any old shell history or doc
# links that invoke ./scripts/misc-preview.sh fail loudly instead
# of silently corrupting settings_data.json.
# ============================================================

echo "misc-preview.sh is deprecated."
echo "The misc_dev_show_price theme setting has been removed."
echo "Price gating is now handled by an external app, not the theme."
exit 1
